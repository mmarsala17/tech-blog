<!DOCTYPE html>
<html lang ="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="Viewport" content="width=device-width, initial-scale=1.0">
        <title>Technology and Esports Blog</title>
        <link rel="stylesheet" href="/public/css/reset.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    </head>
    <body>

        <nav class="navbar navbar-dark bg-primary">
            <div class="container-fluid justify-content-start">

            <a class="navbar-link text-white" href="#">Home</a>

            <a class="navbar-link text-white" href="/dashboard">Dashboard</a>

            {{#unless logged_in}}
            <a class="navbar-link text-white" href="/login">Login</a>
            {{/unless}}

            {{#if logged_in}}
            <button class="btn btn-lg btn-primary" id="logout" type="button">Logout</button>
            {{/if}}
        </div>
        
        <h7 class="ms-4 text-white">Tech Blog</h7>
        </nav>

        <main>
            {{{ Body }}}
        </main>

        {{#if logged_in}}
        <script src="/public/js/logout.js"></script>
        {{/if}}

        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.slim.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.9.2/umd/popper.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>
    </body>
</html>